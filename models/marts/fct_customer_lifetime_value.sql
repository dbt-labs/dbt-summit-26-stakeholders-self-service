select
    customer_id,
    signed_up_date,
    home_region,
    favored_discipline,
    first_order_date,
    last_order_date,
    order_count,
    billable_order_count,
    completed_order_count,
    shop_count,
    lifetime_order_item_count,
    lifetime_gross_revenue_gold,
    lifetime_discount_gold,
    lifetime_net_revenue_gold,
    returned_revenue_gold,
    lifetime_collected_gold,
    lifetime_refunded_gold,
    lifetime_net_collected_gold,
    average_order_value_gold,
    days_to_first_order,
    days_since_last_order,
    has_ordered,
    is_repeat_customer
from {{ ref('int_merlinco_customer_lifetime_rollup') }}
